# Cursor Prompt - Fill Master Plan from Current Codebase

당신은 Next.js/React 프런트엔드 아키텍트이자 Node.js/TypeScript 백엔드 리팩토링 전문가다.

현재 작업은 `ai_office`와 `dev_support`를 하나의 통합 웹 서비스로 재구성하는 프로젝트이며,
최종 방향은 **`dev_support` 중심 통합 + `ai_office` 서버 로직 흡수**다.

현재 작업 루트에는 아래 두 저장소가 함께 있다고 가정한다.

- `ai_office/` : 기존 Discord 기반 AI Office 서비스 원본 참조용
- `dev_support/` : 현재 React/Next.js 기반 업무 도우미 앱, 통합의 최종 타깃

이번 턴의 목적은
**현재 코드베이스를 실제로 스캔하고,
그 결과를 바탕으로 `.cursor/plans/office-unify-master-plan.md`를 구체적으로 채우는 것**이다.

중요:
- 이번 턴은 계획 문서를 채우는 것이 목적이다
- 코드 수정은 하지 마라
- 파일 생성/삭제/리팩토링/패치도 하지 마라
- 추상적인 조언이 아니라, 실제 코드베이스를 근거로 계획 문서를 채워라

---

## 반드시 따를 프로젝트 원칙

1. 최종 제품 본체는 `dev_support`다.
2. `ai_office`는 기능 원천이자 참조 원본이다.
3. 기존 `dev_support` 기능은 깨지면 안 된다.
4. Discord 결합 로직은 웹에 직접 이식하지 않는다.
5. 순수 비즈니스 로직은 가능한 한 재사용 후보로 본다.
6. 민감정보, API Key, Supabase service role key 등은 서버 전용으로 유지해야 한다.
7. 통합은 항상 분석 → 설계 → 구현 → 검증 → 문서화 순서로 진행한다.
8. 이번 단계에서는 구현이 아니라 **실행 가능한 마스터 플랜 작성**이 목표다.

---

## 이번 턴의 핵심 목표

현재 코드베이스를 분석해 아래를 실제 내용으로 정리하라.

1. `dev_support`의 현재 핵심 기능과 구조
2. `ai_office`의 현재 핵심 기능과 구조
3. 두 저장소 간 역할 차이
4. 재사용 가능한 순수 로직 후보
5. Discord 결합 로직 및 직접 이식 금지 대상
6. 웹 전환 가치가 높은 1차 MVP 대상
7. 목표 폴더 구조와 API 방향
8. 단계별 마이그레이션 계획
9. 예상 변경 파일과 리스크
10. 검증 계획과 문서화 계획

---

## 반드시 수행할 작업

### 1. 실제 코드베이스 스캔
아래를 먼저 확인하라.

- `dev_support`의 주요 디렉토리 구조
- `dev_support`의 라우트/페이지 구조
- `dev_support`의 기존 기능 진입점
- `ai_office`의 주요 디렉토리 구조
- `ai_office`의 핵심 서비스/로직 위치
- Discord 결합 로직이 집중된 파일/영역
- 외부 API / LLM / DB 접근이 집중된 영역

### 2. 구조 분류
아래 기준으로 분류하라.

#### A. 순수 로직
- 웹/Discord에 직접 묶이지 않은 재사용 가능한 로직

#### B. 서버 전용 로직
- 외부 API 호출
- LLM provider
- DB 접근
- 민감정보 사용 로직

#### C. Discord 결합 로직
- interaction
- customId
- panel restore
- ephemeral reply
- Discord message formatting

#### D. 운영 전용 로직
- 운영 패널
- 상태 확인
- 제어 액션
- 운영 알림/재기동 흐름

### 3. 목표 구조 제안
- `dev_support` 중심의 통합 제품 구조
- 서버 계층 제안
- API 설계 방향
- IA/라우팅 방향
- 상태 관리 방향

### 4. 단계별 마이그레이션 계획 작성
- Phase별 목표
- Phase별 예상 변경 범위
- Phase별 완료 조건
- Phase별 리스크

---

## 작성 대상 문서

아래 문서를 채운다고 가정하고 작성하라.

`.cursor/plans/office-unify-master-plan.md`

작성 결과는 아래 항목을 실제 내용으로 채워야 한다.

---

## 반드시 채워야 할 항목

### 1. 작업 개요
- 작업명
- 작업 목적
- 작업 범위
- 포함 범위 / 제외 범위

### 2. 현재 구조 분석 요약
- `dev_support` 분석 요약
- `ai_office` 분석 요약
- 통합 관점 요약

### 3. 기능 분류
- 순수 로직
- 서버 전용 로직
- Discord 결합 로직
- 운영 기능

### 4. 목표 아키텍처 초안
- 최상위 구조
- 제안 폴더 구조
- 클라이언트/서버 경계
- 보안상 주의점

### 5. 라우팅 / IA 초안
- 주요 라우트
- 공통 UI 방향
- 섹션별 역할

### 6. 상태 관리 전략
- 클라이언트 상태
- 서버 상태
- source of truth
- 캐시/임시 저장 전략

### 7. API 설계 초안
- 우선 추가할 API
- 각 API의 목적/입력/출력/보안 포인트

### 8. 단계별 구현 계획
- Phase 0. 분석/설계
- Phase 1. 구조 골격 추가
- Phase 2. 포트폴리오 조회 이관
- Phase 3. AI 토론 실행 이관
- Phase 4. 트렌드 분석 실행 이관
- Phase 5. 운영 상태 읽기 페이지
- Phase 6. 문서화 및 정리

### 9. 변경 파일 예측
- 신규 파일 예상
- 수정 파일 예상
- 아직 미정인 파일

### 10. 검증 계획
- 기본 검증
- 기능 검증
- 검증 보류 가능 항목

### 11. 리스크 로그
- 현재 리스크
- 구조적 주의점
- 보안 주의점

### 12. 이번 단계 완료 기준
- 설계 완성도
- 구현 가능성
- 범위 적정성
- 기존 기능 보호 계획
- 검증/문서 계획 포함 여부

---

## 작성 원칙

### 1. 실제 파일/구조를 근거로 써라
막연한 일반론이 아니라,
현재 코드베이스에서 확인한 구조와 파일을 근거로 작성하라.

### 2. 확인한 것과 추정을 구분하라
- 실제로 확인한 구조
- 아직 코드 전체를 보지 못해 추정한 부분
을 분리해서 적어라.

### 3. 구현 가능한 수준으로 써라
이 문서는 “좋아 보이는 제안서”가 아니라
실제 다음 단계 구현에 바로 쓰일 계획 문서여야 한다.

### 4. 범위를 넓히지 마라
전체 이상론보다
1차 MVP와 점진적 마이그레이션 중심으로 써라.

### 5. 리스크를 숨기지 마라
구조적 불확실성, 보안 우려, 회귀 위험이 있으면 분명히 적어라.

---

## 출력 형식

반드시 아래 형식으로 정리하라.

### 1. 코드베이스 분석 요약
- `dev_support` 요약
- `ai_office` 요약
- 통합 핵심 포인트

### 2. `.cursor/plans/office-unify-master-plan.md` 초안
아래 항목을 순서대로 채워라.
- 작업 개요
- 현재 구조 분석 요약
- 기능 분류
- 목표 아키텍처 초안
- 라우팅 / IA 초안
- 상태 관리 전략
- API 설계 초안
- 단계별 구현 계획
- 변경 파일 예측
- 검증 계획
- 리스크 로그
- 이번 단계 완료 기준

### 3. 확인된 것 vs 추정한 것
- 확인된 것:
- 추정한 것:

### 4. 가장 안전한 다음 단계
- 다음 턴에서 가장 먼저 할 것
- 그 이유
- 가장 작은 구현 단위 제안

---

## 금지 사항

- 코드 수정 금지
- 파일 생성 금지
- 구현 시작 금지
- 일반론만 반복 금지
- 실제 코드베이스를 안 본 상태의 막연한 구조 제안 금지
- 리스크 없는 낙관적 결론 금지

---

## 최종 목적

이번 턴의 목적은
**현재 코드베이스를 근거로, 실제 구현에 바로 사용할 수 있는 마스터 플랜 문서를 완성도 높게 채우는 것**이다.

즉,
- 무엇을 통합할지
- 무엇을 보류할지
- 무엇을 먼저 옮길지
- 어디가 위험한지
- 어떤 파일이 바뀔지

를 실행 가능한 수준으로 정리하라.

출력은 한국어로 작성하라.
